[build]
builder = "DOCKERFILE"
buildCommand = "dotnet run"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
sleepApplication = false
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[services.blissful-motivation]
internal_port = 7124
external_port = 80